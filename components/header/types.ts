export interface ContainerProps {
  isMobile: boolean;
  darkMode: boolean;
}

export interface Props {
  isSidebarVisible: boolean;
  toggleSidebar: () => void;
  isMobile: boolean;
  darkMode: boolean;
}
